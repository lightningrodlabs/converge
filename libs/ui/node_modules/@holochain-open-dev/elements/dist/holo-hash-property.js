import { decodeHashFromBase64 } from "@holochain/client";
export function hashState() {
    return {
        hasChanged: (oldVal, newVal) => oldVal?.toString() !== newVal?.toString(),
    };
}
export function hashProperty(attributeName) {
    return {
        attribute: attributeName,
        type: Object,
        hasChanged: (oldVal, newVal) => oldVal?.toString() !== newVal?.toString(),
        converter: (s) => s && s.length > 0 && decodeHashFromBase64(s),
    };
}
//# sourceMappingURL=holo-hash-property.js.map