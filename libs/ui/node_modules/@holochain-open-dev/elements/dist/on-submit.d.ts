import { AttributePart } from "lit";
import { Directive } from "lit/directive.js";
/**
 * Serializes a form and returns a plain object. If a form control with the same name appears more than once, the
 * property will be converted to an array.
 */
export declare function serialize(form: HTMLFormElement): Record<string, unknown>;
declare class OnSubmitDirective extends Directive {
    listener: (e: SubmitEvent) => any | undefined;
    initialized: boolean;
    update(part: AttributePart, props: unknown[]): void;
    render(_onSubmitCallback: (formData: any) => void): string;
}
export declare const onSubmit: (_onSubmitCallback: (formData: any) => void) => import("lit-html/directive").DirectiveResult<typeof OnSubmitDirective>;
export {};
