import { LitElement, PropertyValues } from "lit";
import { HoloHash } from "@holochain/client";
import "@shoelace-style/shoelace/dist/components/tooltip/tooltip.js";
export declare class HoloIdenticon extends LitElement {
    hash: HoloHash;
    /**
     * Size of the identicon in pixels.
     */
    size: number;
    /**
     * Shape of the identicon.
     */
    shape: "square" | "circle";
    /**
     * Disables showing the tooltip for the hash
     */
    disableTooltip: boolean;
    /**
     * Disable copying of the hash on click
     */
    disableCopy: boolean;
    private _canvas;
    private _tooltip;
    justCopiedHash: boolean;
    timeout: any;
    copyHash(): Promise<void>;
    get strHash(): string;
    updated(changedValues: PropertyValues): void;
    renderCanvas(): import("lit-html").TemplateResult<1>;
    render(): import("lit-html").TemplateResult<1>;
    static get styles(): import("lit").CSSResult;
}
