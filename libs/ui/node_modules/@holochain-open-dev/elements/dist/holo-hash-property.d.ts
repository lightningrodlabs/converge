import { HoloHash } from "@holochain/client";
import { PropertyDeclaration } from "lit";
export declare function hashState(): {
    hasChanged: (oldVal: HoloHash, newVal: HoloHash) => boolean;
};
export declare function hashProperty(attributeName: string): PropertyDeclaration<Object | null, unknown>;
