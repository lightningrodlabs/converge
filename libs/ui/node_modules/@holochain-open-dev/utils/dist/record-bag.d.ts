import { Action, ActionHash, Record } from "@holochain/client";
import { ActionHashMap, AgentPubKeyMap, EntryHashMap } from "./holo-hash-map.js";
import { EntryRecord } from "./entry-record.js";
export declare class RecordBag<T> {
    protected records: Record[];
    entryMap: EntryHashMap<T>;
    actionMap: ActionHashMap<Action>;
    entryActions: EntryHashMap<Uint8Array[]>;
    authorMap: AgentPubKeyMap<Uint8Array[]>;
    updates: ActionHashMap<Uint8Array[]>;
    deletes: ActionHashMap<Uint8Array[]>;
    get entriesByAuthor(): AgentPubKeyMap<T[]>;
    get entryRecords(): EntryRecord<T>[];
    entryRecord(actionHash: ActionHash): EntryRecord<T> | undefined;
    constructor(records?: Record[]);
    add(records: Record[]): void;
    addBag(recordBag: RecordBag<T>): void;
}
