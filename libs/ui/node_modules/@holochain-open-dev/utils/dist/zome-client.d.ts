import { AppAgentClient } from "@holochain/client";
import { UnsubscribeFunction } from "emittery";
export declare class ZomeClient<SIGNAL_PAYLOAD> {
    client: AppAgentClient;
    roleName: string;
    zomeName: string;
    constructor(client: AppAgentClient, roleName: string, zomeName: string);
    onSignal(listener: (eventData: SIGNAL_PAYLOAD) => void | Promise<void>): UnsubscribeFunction;
    protected callZome(fn_name: string, payload: any): Promise<any>;
}
