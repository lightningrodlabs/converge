import { HoloHash } from "@holochain/client";
import { Readable } from "svelte/store";
import { AsyncReadable } from "./async-readable.js";
type StoreValue<T> = T extends Readable<infer U> ? U : never;
type AsyncStoreValue<T> = T extends AsyncReadable<infer U> ? U : never;
export declare function joinMap<H extends HoloHash, T extends Readable<any>>(holoHashMap: ReadonlyMap<H, T>): Readable<ReadonlyMap<H, StoreValue<T>>>;
export declare function joinAsyncMap<H extends HoloHash, T extends AsyncReadable<any>>(holoHashMap: ReadonlyMap<H, T>): AsyncReadable<ReadonlyMap<H, AsyncStoreValue<T>>>;
export {};
