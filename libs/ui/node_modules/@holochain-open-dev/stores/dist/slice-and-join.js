import { HoloHashMap, slice } from "@holochain-open-dev/utils";
import { joinAsyncMap } from "./join-map.js";
// Takes a map of AsyncReadables, it gets the stores for the given hashes,
// and returns those stores joined
export function sliceAndJoin(map, hashes) {
    const s = slice(map, hashes);
    const hs = new HoloHashMap(Array.from(s.entries()).filter(([h, v]) => v !== undefined));
    return joinAsyncMap(hs);
}
//# sourceMappingURL=slice-and-join.js.map