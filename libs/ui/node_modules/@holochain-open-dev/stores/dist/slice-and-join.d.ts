import { GetonlyMap } from "@holochain-open-dev/utils";
import { HoloHash } from "@holochain/client";
import { AsyncReadable } from "./async-readable.js";
export type Option<T> = T | undefined;
export declare function sliceAndJoin<H extends HoloHash, T>(map: GetonlyMap<H, AsyncReadable<Option<T>>>, hashes: Array<H>): AsyncReadable<ReadonlyMap<H, T>>;
